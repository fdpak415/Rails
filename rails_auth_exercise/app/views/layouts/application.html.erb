<!DOCTYPE html>
<html>
  <head>
    <title>RailsAuthExercise</title>
    <% if !current_user.nil?  %>

    <ul>
      <li>Loggin in as: <%= current_user.username %></li>
      <li> <%= button_to "Logout", session_url, method: :delete %></li>
    </ul>
    <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
